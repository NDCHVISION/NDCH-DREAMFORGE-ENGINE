{
  "name": "ndch-dreamforge-engine",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "tsx --test env.test.ts reel-plan.test.ts ai-enrichment.test.ts subtitles-timing.test.ts reel-validation.test.ts music-analysis.test.ts",
    "music:analyze": "sh -c 'if [ -f .env ]; then exec tsx --env-file=.env scripts/analyze-reel-music.ts; else exec tsx scripts/analyze-reel-music.ts; fi'",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "generate": "sh -c 'if [ -f .env ]; then exec tsx --env-file=.env generate-reel.ts; else exec tsx generate-reel.ts; fi'",
    "publish": "sh -c 'if [ -f .env ]; then exec tsx --env-file=.env publish-reel.ts; else exec tsx publish-reel.ts; fi'"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@playwright/test": "^1.49.1",
    "@types/node": "^25.6.2",
    "eslint": "^9.39.4",
    "prettier": "^3.8.3",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.2"
  }
}
